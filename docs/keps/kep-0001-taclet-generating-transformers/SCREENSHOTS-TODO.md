# Screenshots to capture for KEP-1

The four `*.png` files in this directory are **labeled placeholders**. Replace
each with a real screenshot captured from KeY (transparent-lemmas branch).
Keep the same file names so `index.md` picks them up.

| File | Where | What to show |
|------|-------|--------------|
| `strategy-option.png` | Proof Search Strategy tab | The "One Step Simplification" option with the three radio buttons **Enabled / Disabled / Transparent** (hover the Transparent tooltip if it reads well). |
| `prooftree-transparent.png` | Main window proof tree, a proof done in **Transparent** mode | A stretch of the tree showing `introduce_ossLemma` and `ossLemma_…` pairs and ordinary steps — and **no** "One Step Simplification" nodes. SumAndMax or BinarySearch work well. Optionally a second image of the same proof in **Enabled** mode for side-by-side contrast. |
| `po-browser-lemmas.png` | Proof Management → **By Proof** → **Lemmas** tab | The generator/content tree ("ossLemma — N lemma(s), M distinct (x/x proven)") with the **Prove all lemmas…** and **Load selected as side proofs** buttons. (This is the view from the earlier BinarySearch screenshots.) |
| `prove-all-progress.png` | During **Prove all lemmas…** | The "Proving Lemmas" progress dialog with the "done / total" bar and the **Cancel** button. |

Suggested capture settings: default (Material) look, window sized so the
relevant panel fills the frame; PNG; roughly 700–1000 px wide is plenty.
